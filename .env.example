# =============================================================================
# DIAL Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values.
# The DIAL framework itself doesn't require environment variables to run;
# they're only needed for specific integrations.
# =============================================================================

# -----------------------------------------------------------------------------
# ANTHROPIC (Primary LLM Provider)
# -----------------------------------------------------------------------------
# Your Anthropic API key for accessing Claude models.
# Get your key at: https://console.anthropic.com/settings/keys
# Required when using DIAL with Anthropic models.
ANTHROPIC_API_KEY=sk-ant-...

# -----------------------------------------------------------------------------
# OPENROUTER (Alternative LLM Provider)
# -----------------------------------------------------------------------------
# OpenRouter provides access to multiple LLM providers through a unified
# OpenAI-compatible API. Use this as an alternative to direct Anthropic access.
# Get your key at: https://openrouter.ai/keys

# Your OpenRouter API token (required when using OpenRouter)
OPENROUTER_API_TOKEN=

# The model to use with OpenRouter (optional)
# Examples: openai/gpt-4o, anthropic/claude-3-opus, meta-llama/llama-3-70b
# Default: openai/gpt-4o-mini
# OPENROUTER_MODEL=openai/gpt-4o-mini

# -----------------------------------------------------------------------------
# WEBHOOK SPECIALISTS (Custom Integration Tokens)
# -----------------------------------------------------------------------------
# When registering specialists with remote webhooks (strategyWebhookUrl or
# contextWebhookUrl), you can specify authentication tokens via environment
# variables. The variable name is configured per-specialist using the
# `webhookTokenName` parameter.
#
# Example: If you register a specialist with webhookTokenName: "MY_SERVICE_TOKEN"
# then set that variable here:
#
# MY_SERVICE_TOKEN=your-secret-token-here
#
# These tokens are used for HTTP Basic Auth:
#   Authorization: Basic base64("machineName:${TOKEN}")

# -----------------------------------------------------------------------------
# PROXY MODE (Remote MCP Server)
# -----------------------------------------------------------------------------
# DIAL can act as an HTTP proxy to a remote MCP server, or expose itself
# as an HTTP endpoint for remote clients.

# Forward all requests to a remote MCP server at this URL.
# When set, dialai CLI and dialai-mcp will forward tool calls to this server.
# Example: http://remote-server:3000
# DIALAI_BASE_URL=

# Port to expose HTTP endpoint (for acting as remote server).
# When set, dialai-mcp will start an HTTP server alongside stdio transport.
# Example: 3000
# DIALAI_PORT=

# Auth token for proxy mode (sent as Bearer token, validated on receive).
# Used for both client and server authentication.
# DIALAI_API_TOKEN=
